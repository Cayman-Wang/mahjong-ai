# 第二轮审查-2（更新版）

**审查日期**: 2026-02-13  
**范围**: RLModule 迁移收尾、训练构建接口、checkpoint 续训链路、遗留死代码与回归测试。

## 1) 结论

- **P0**: 0
- **P1**: 0
- **P2**: 1（仅剩 Ray FutureWarning / 新 API 提示告警，不影响功能）
- **是否可合并**: ✅ 可合并

## 2) Findings 复核与处理结果

### Finding A: `build_algo/build` 迁移与兼容（已修复）
- **状态**: ✅ 已处理
- **位置**: `src/mahjong_ai/training/rllib_runner.py:166`
- **说明**:
  - 增加 `_build_algorithm()`，优先使用非 deprecated 构建路径。
  - 对 Ray 2.53（`build` 是 deprecated wrapper `_ctor`）自动走 `build_algo()`，避免 `build` 的 deprecation 日志。
  - 对新版本保留 `build()` 路径兼容。

### Finding B: restore 失败时清理路径遗漏（已修复）
- **状态**: ✅ 已处理
- **位置**: `src/mahjong_ai/training/rllib_runner.py:439`
- **说明**:
  - `resume_from` 的 restore 操作已纳入统一 `try/finally`。
  - 即便 restore 抛错也会执行 `algo.stop()` 和 `ray.shutdown()`。

### Finding C: `resume_from` 仅支持本地路径（已修复）
- **状态**: ✅ 已处理
- **位置**: `src/mahjong_ai/training/rllib_runner.py:193`
- **说明**:
  - 增加 `_normalize_resume_from()`，支持 `s3://...` 等 URI 直接透传。
  - 本地路径仍做存在性校验。

### Finding D: 旧 ModelV2 死代码残留（已修复）
- **状态**: ✅ 已处理
- **位置**: `src/mahjong_ai/training/rllib_masked_model.py`（已删除）
- **说明**:
  - 删除旧 `TorchModelV2` 实现，避免与 RLModule 主链路语义冲突。
  - 测试文件重命名为 `tests/test_rllib_action_mask_rl_module.py`。

### Finding E: RLModule deprecation 噪声（已修复）
- **状态**: ✅ 已处理
- **位置**: `src/mahjong_ai/training/rllib_action_mask_rl_module.py:37`
- **说明**:
  - 在模块侧对 `RLModuleConfig.__init__` 的 Deprecated wrapper 做一次性解包。
  - RLModule 单测中不再出现 `RLModule(config=[...])` 误报告警。

## 3) 新增/更新测试

- `tests/test_training_runner.py:67`
  - 新增 `test_normalize_resume_from_remote_uri_passthrough`
  - 新增 `_build_algorithm` 的 build / build_algo 分支测试
  - 新增 `test_train_with_rllib_restore_failure_still_cleans_up`（restore 失败清理保障）
  - 新增 `test_train_with_rllib_resume_from_uri_passthrough`（URI 续训链路）
- `tests/test_training_runner_rllib_smoke.py:22`
  - 新增 RL 端到端 smoke：1 次训练 + checkpoint + `resume_from` 续训
- `tests/test_rllib_action_mask_rl_module.py:1`
  - 已替换原 `test_rllib_masked_model.py` 文件名

## 4) 关键变更清单（代码）

- `src/mahjong_ai/training/rllib_runner.py:166`
  - 新增 `_build_algorithm()` 与 `_normalize_resume_from()`
  - 新增 `_install_ray_warning_filters()`，抑制 Ray 上游 logger deprecation 噪声
  - 去除 runner 内部的 RLModuleConfig monkey patch
  - restore 放入统一清理保护块
- `src/mahjong_ai/training/rllib_action_mask_rl_module.py:37`
  - 模块内补丁清理 RLModuleConfig deprecation 噪声
- `tests/test_training_runner_rllib_smoke.py:1`
  - 新增端到端训练/续训回归
- `src/mahjong_ai/training/rllib_masked_model.py`
  - 删除

## 5) 验证记录

### 本轮必跑
- `PYTHONPATH=src python -m unittest discover -s tests -v`  
  - 结果：`Ran 59 tests ... OK (skipped=8)`
- `PYTHONPATH=src python -m mahjong_ai.cli.main sim --games 5 --seed 1`  
  - 结果：`games=5 avg_scores=[19.0, -4.8, -8.4, -5.8] total_steps=618`

### RL 专项补跑
- `PYTHONPATH=src /usr/bin/python3 -m unittest tests.test_training_runner_rllib_smoke -v`  
  - 结果：训练+续训 smoke 通过
- `PYTHONPATH=src /usr/bin/python3 -m mahjong_ai.cli.main train-rllib ...`（1 iter）  
  - 结果：训练成功并产出 checkpoint；`UnifiedLogger/Json/CSV/TBX` deprecation 告警已抑制

## 6) 残余风险（非阻塞）

- Ray 2.53 在本地仍会输出 `FutureWarning`（`RAY_ACCEL_ENV_VAR_OVERRIDE_ON_ZERO`）与“new API stack”提示日志；不影响训练正确性。
- 如需“控制台零告警”，后续可继续评估 Ray 运行时环境变量与日志级别精细化配置。
